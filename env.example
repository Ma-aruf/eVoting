# Django Settings
SECRET_KEY=your-secret-key-here
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1

# Railway Environment Variables (auto-set by Railway)
RAILWAY_ENVIRONMENT=
RAILWAY_PUBLIC_DOMAIN=
RAILWAY_FRONTEND_URL=
RAILWAY_ALLOW_ALL_CORS=false

# Database Configuration
# For Railway: DATABASE_URL is auto-set when you add Railway PostgreSQL service
# For Local: Uses the hardcoded PostgreSQL settings below
DATABASE_URL=postgres://user:password@host:port/dbname

# Local Development Database (hardcoded in settings.py)
# PGDATABASE=evkasec_db
# PGUSER=postgres  
# PGPASSWORD=@nasarabieni
# PGHOST=localhost
# PGPORT=5432

# Cloudinary Configuration (for production image uploads)
# In development, images are saved locally to /media folder
# In production, set these to enable Cloudinary uploads
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
